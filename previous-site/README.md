# Previous Site

This folder preserves the old Talking Pacers homepage before the temporary AI/chat landing page swap.

To restore the old homepage:

```bash
cp previous-site/index.html index.html
```

The old homepage still relies on the existing root-level `script.js`, `styles.css`, `data.json`, and `components/RememberThisCard.jsx` files.
